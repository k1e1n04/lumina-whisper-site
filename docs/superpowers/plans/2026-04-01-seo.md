# SEO対策 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** lumina-whisper-site の SEO を包括的に改善する。ページ別メタタグの SSR 注入・OGP 画像生成・JSON-LD 構造化データ・sitemap.xml を整備する。

**Architecture:** `main.tsx` はすでに `BrowserRouter`（basename 付き）と `hydrateRoot` を使用しており、SSG インフラも完成済み（entry-server.tsx + scripts/prerender.mjs）。`react-helmet-async` を追加して各ページに `<Helmet>` を埋め込み、`prerender.mjs` でプリレンダリング時に helmet 値を静的 HTML へ注入する。OGP 画像は `@resvg/resvg-js` で SVG → PNG に変換してビルド時に生成する。

**Tech Stack:** React 18, TypeScript, Vite, react-router-dom v7, react-helmet-async, @resvg/resvg-js, vitest, @testing-library/react

**Base URL:** `https://k1e1n04.github.io/lumina-whisper-site`

---

## ファイル構成（変更・新規作成一覧）

| ファイル | 変更種別 | 内容 |
|---------|---------|------|
| `src/App.tsx` | 変更 | デッドコードの `HashRouter` を `BrowserRouter` に置換 |
| `src/main.tsx` | 変更 | `HelmetProvider` で wrap |
| `src/entry-server.tsx` | 変更 | `HelmetProvider` 追加・helmet state を返却するよう変更 |
| `index.html` | 変更 | title/meta を `<!--helmet-tags-->` プレースホルダーに置換 |
| `scripts/prerender.mjs` | 変更 | helmet 注入 + sitemap.xml 生成を追加 |
| `scripts/generate-ogp.mjs` | 新規作成 | OGP 画像 SVG → PNG 生成スクリプト |
| `src/pages/LandingPage.tsx` | 変更 | `<Helmet>` + JSON-LD 追加 |
| `src/pages/ReleasePage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/InstallPage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/SetupPage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/UsagePage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/SettingsPage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/FaqPage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/pages/guide/TroubleshootPage.tsx` | 変更 | `<Helmet>` 追加 |
| `src/test/meta.test.tsx` | 新規作成 | ページ別 title/description テスト |
| `public/robots.txt` | 新規作成 | クローラー向け設定 |
| `package.json` | 変更 | build スクリプト更新・依存パッケージ追加 |

---

## Task 1: 依存パッケージのインストール + robots.txt 作成

**Files:**
- Modify: `package.json`
- Create: `public/robots.txt`

- [ ] **Step 1: react-helmet-async と @resvg/resvg-js をインストールする**

```bash
cd /Users/ishiiken/Develop/lumina-whisper-site
npm install react-helmet-async
npm install -D @resvg/resvg-js
```

Expected: `package.json` の `dependencies` に `react-helmet-async`、`devDependencies` に `@resvg/resvg-js` が追加される。

- [ ] **Step 2: robots.txt を作成する**

`public/robots.txt` を作成する：

```
User-agent: *
Allow: /
Sitemap: https://k1e1n04.github.io/lumina-whisper-site/sitemap.xml
```

- [ ] **Step 3: 既存テストが通ることを確認する**

```bash
npm run test:run
```

Expected: 全テスト PASS。

- [ ] **Step 4: コミットする**

```bash
git add package.json package-lock.json public/robots.txt
git commit -m "chore: add react-helmet-async, @resvg/resvg-js, robots.txt"
```

---

## Task 2: App.tsx のデッドコード削除

**Files:**
- Modify: `src/App.tsx`

**背景:** `main.tsx` はすでに `BrowserRouter`（basename="/lumina-whisper-site"）を提供し `<App routerless />` と呼び出している。`App.tsx` の `HashRouter` ブランチは一切使われていないデッドコード。

- [ ] **Step 1: 既存ルーティングテストが通ることを確認する（ベースライン）**

```bash
npm run test:run -- src/test/routing.test.tsx
```

Expected: 3テスト全て PASS。

- [ ] **Step 2: App.tsx の HashRouter を BrowserRouter に置換する**

`src/App.tsx` の該当箇所を変更する：

変更前：
```tsx
import { HashRouter, Navigate, Route, Routes } from 'react-router-dom'
```

変更後：
```tsx
import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'
```

変更前（return部分）：
```tsx
  return (
    <HashRouter>
      <AppRoutes />
    </HashRouter>
  )
```

変更後：
```tsx
  return (
    <BrowserRouter basename="/lumina-whisper-site">
      <AppRoutes />
    </BrowserRouter>
  )
```

- [ ] **Step 3: テストが引き続き通ることを確認する**

```bash
npm run test:run -- src/test/routing.test.tsx
```

Expected: 3テスト全て PASS（テスト側は `MemoryRouter` + `routerless` を使うため変更不要）。

- [ ] **Step 4: コミットする**

```bash
git add src/App.tsx
git commit -m "refactor: replace dead HashRouter with BrowserRouter in App.tsx"
```

---

## Task 3: meta テストを書く（失敗確認）

**Files:**
- Create: `src/test/meta.test.tsx`

- [ ] **Step 1: meta.test.tsx を作成する**

```tsx
// src/test/meta.test.tsx
import { describe, it, expect, beforeEach } from 'vitest'
import { render, waitFor } from '@testing-library/react'
import { HelmetProvider } from 'react-helmet-async'
import { MemoryRouter } from 'react-router-dom'
import '../i18n'
import LandingPage from '../pages/LandingPage'
import ReleasePage from '../pages/ReleasePage'
import InstallPage from '../pages/guide/InstallPage'
import SetupPage from '../pages/guide/SetupPage'
import UsagePage from '../pages/guide/UsagePage'
import SettingsPage from '../pages/guide/SettingsPage'
import FaqPage from '../pages/guide/FaqPage'
import TroubleshootPage from '../pages/guide/TroubleshootPage'

function renderWithHelmet(ui: React.ReactElement) {
  return render(
    <HelmetProvider>
      <MemoryRouter>{ui}</MemoryRouter>
    </HelmetProvider>,
  )
}

describe('page titles', () => {
  beforeEach(() => {
    document.title = ''
  })

  it('LandingPage', async () => {
    renderWithHelmet(<LandingPage />)
    await waitFor(() => {
      expect(document.title).toBe(
        'Mac 音声入力アプリ — オフライン・高精度 | Lumina Whisper',
      )
    })
  })

  it('ReleasePage', async () => {
    renderWithHelmet(<ReleasePage />)
    await waitFor(() => {
      expect(document.title).toBe('リリースノート | Lumina Whisper')
    })
  })

  it('InstallPage', async () => {
    renderWithHelmet(<InstallPage />)
    await waitFor(() => {
      expect(document.title).toBe('インストール | Lumina Whisper ガイド')
    })
  })

  it('SetupPage', async () => {
    renderWithHelmet(<SetupPage />)
    await waitFor(() => {
      expect(document.title).toBe('初期設定 | Lumina Whisper ガイド')
    })
  })

  it('UsagePage', async () => {
    renderWithHelmet(<UsagePage />)
    await waitFor(() => {
      expect(document.title).toBe('基本的な使い方 | Lumina Whisper ガイド')
    })
  })

  it('SettingsPage', async () => {
    renderWithHelmet(<SettingsPage />)
    await waitFor(() => {
      expect(document.title).toBe('設定項目 | Lumina Whisper ガイド')
    })
  })

  it('FaqPage', async () => {
    renderWithHelmet(<FaqPage />)
    await waitFor(() => {
      expect(document.title).toBe('よくある質問 | Lumina Whisper ガイド')
    })
  })

  it('TroubleshootPage', async () => {
    renderWithHelmet(<TroubleshootPage />)
    await waitFor(() => {
      expect(document.title).toBe(
        'トラブルシューティング | Lumina Whisper ガイド',
      )
    })
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
npm run test:run -- src/test/meta.test.tsx
```

Expected: 全8テスト FAIL（`document.title` が空文字のまま）。

---

## Task 4: HelmetProvider 統合（main.tsx + entry-server.tsx + index.html + prerender.mjs）

**Files:**
- Modify: `src/main.tsx`
- Modify: `src/entry-server.tsx`
- Modify: `index.html`
- Modify: `scripts/prerender.mjs`

- [ ] **Step 1: main.tsx に HelmetProvider を追加する**

`src/main.tsx` を以下に変更する：

```tsx
import { StrictMode } from 'react'
import { createRoot, hydrateRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import { HelmetProvider } from 'react-helmet-async'
import './index.css'
import './i18n'
import App from './App'

const container = document.getElementById('root')!
const app = (
  <StrictMode>
    <HelmetProvider>
      <BrowserRouter basename="/lumina-whisper-site">
        <App routerless />
      </BrowserRouter>
    </HelmetProvider>
  </StrictMode>
)

if (container.hasChildNodes()) {
  hydrateRoot(container, app)
} else {
  createRoot(container).render(app)
}
```

- [ ] **Step 2: entry-server.tsx を更新して helmet state を返却する**

`src/entry-server.tsx` を以下に変更する：

```tsx
import { StrictMode } from 'react'
import { renderToString } from 'react-dom/server'
import { StaticRouter } from 'react-router-dom'
import { HelmetProvider } from 'react-helmet-async'
import type { FilledContext } from 'react-helmet-async'
import './i18n'
import App from './App'

export function render(url: string): { html: string; helmet: FilledContext['helmet'] } {
  const helmetContext: Partial<FilledContext> = {}
  const html = renderToString(
    <StrictMode>
      <HelmetProvider context={helmetContext}>
        <StaticRouter location={url}>
          <App routerless />
        </StaticRouter>
      </HelmetProvider>
    </StrictMode>,
  )
  return { html, helmet: (helmetContext as FilledContext).helmet }
}
```

- [ ] **Step 3: index.html のメタタグをプレースホルダーに置換する**

`index.html` を以下に変更する（既存の title・meta タグをすべて `<!--helmet-tags-->` 1行に置換）：

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/lumina-whisper-site/icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!--helmet-tags-->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Shippori+Mincho+B1:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  </head>
  <body>
    <div id="root"><!--ssr-outlet--></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 4: prerender.mjs を更新して helmet を HTML へ注入する**

`scripts/prerender.mjs` を以下に変更する：

```js
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const root = path.resolve(__dirname, '..')
const BASE_URL = 'https://k1e1n04.github.io/lumina-whisper-site'

const routes = [
  { url: '/', priority: '1.0' },
  { url: '/release', priority: '0.6' },
  { url: '/guide/install', priority: '0.8' },
  { url: '/guide/setup', priority: '0.8' },
  { url: '/guide/usage', priority: '0.8' },
  { url: '/guide/settings', priority: '0.7' },
  { url: '/guide/faq', priority: '0.7' },
  { url: '/guide/troubleshoot', priority: '0.7' },
]

const template = fs.readFileSync(path.join(root, 'dist/index.html'), 'utf-8')
const { render } = await import(path.join(root, 'dist-ssr/entry-server.js'))

console.log('Pre-rendering routes...')
for (const { url } of routes) {
  const { html: appHtml, helmet } = render(url)

  const helmetTags = [
    helmet.title.toString(),
    helmet.meta.toString(),
    helmet.link.toString(),
    helmet.script.toString(),
  ]
    .filter(Boolean)
    .join('\n    ')

  const html = template
    .replace('<!--ssr-outlet-->', appHtml)
    .replace('<!--helmet-tags-->', helmetTags)

  const outFile =
    url === '/'
      ? path.join(root, 'dist/index.html')
      : path.join(root, `dist${url}/index.html`)

  fs.mkdirSync(path.dirname(outFile), { recursive: true })
  fs.writeFileSync(outFile, html)
  console.log(`  ✓ ${url}`)
}

// Generate sitemap.xml
const sitemapEntries = routes
  .map(
    ({ url, priority }) =>
      `  <url>\n    <loc>${BASE_URL}${url === '/' ? '/' : url + '/'}</loc>\n    <priority>${priority}</priority>\n  </url>`,
  )
  .join('\n')

const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${sitemapEntries}
</urlset>`

fs.writeFileSync(path.join(root, 'dist/sitemap.xml'), sitemap)
console.log('  ✓ sitemap.xml')

// Clean up SSR build artifacts
fs.rmSync(path.join(root, 'dist-ssr'), { recursive: true })
console.log('Done.')
```

---

## Task 5: LandingPage に Helmet + JSON-LD を追加する

**Files:**
- Modify: `src/pages/LandingPage.tsx`

- [ ] **Step 1: LandingPage.tsx に Helmet を追加する**

`src/pages/LandingPage.tsx` の先頭 import に追加し、`return` の中に `<Helmet>` ブロックを挿入する。

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`<Navbar />` の直前（`return` の中の最初の要素として）に挿入：

```tsx
      <Helmet>
        <title>Mac 音声入力アプリ — オフライン・高精度 | Lumina Whisper</title>
        <meta
          name="description"
          content="Lumina Whisper は Mac 専用の音声入力アプリ。完全ローカル処理でプライバシーを守りながら高精度な文字起こしを実現。Apple Silicon 最適化、インターネット不要で高速動作。"
        />
        <meta property="og:title" content="Mac 音声入力アプリ — オフライン・高精度 | Lumina Whisper" />
        <meta
          property="og:description"
          content="Lumina Whisper は Mac 専用の音声入力アプリ。完全ローカル処理でプライバシーを守りながら高精度な文字起こしを実現。Apple Silicon 最適化、インターネット不要で高速動作。"
        />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="https://k1e1n04.github.io/lumina-whisper-site/" />
        <meta property="og:image" content="https://k1e1n04.github.io/lumina-whisper-site/ogp.png" />
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content="Mac 音声入力アプリ — オフライン・高精度 | Lumina Whisper" />
        <meta
          name="twitter:description"
          content="Lumina Whisper は Mac 専用の音声入力アプリ。完全ローカル処理でプライバシーを守りながら高精度な文字起こしを実現。Apple Silicon 最適化、インターネット不要で高速動作。"
        />
        <meta name="twitter:image" content="https://k1e1n04.github.io/lumina-whisper-site/ogp.png" />
        <link rel="canonical" href="https://k1e1n04.github.io/lumina-whisper-site/" />
        <link rel="alternate" hrefLang="ja" href="https://k1e1n04.github.io/lumina-whisper-site/" />
        <link rel="alternate" hrefLang="en" href="https://k1e1n04.github.io/lumina-whisper-site/" />
        <link rel="alternate" hrefLang="x-default" href="https://k1e1n04.github.io/lumina-whisper-site/" />
        <script type="application/ld+json">
          {JSON.stringify({
            '@context': 'https://schema.org',
            '@type': 'SoftwareApplication',
            name: 'Lumina Whisper',
            operatingSystem: 'macOS 13.0+',
            applicationCategory: 'UtilitiesApplication',
            offers: {
              '@type': 'Offer',
              price: '0',
              priceCurrency: 'JPY',
            },
            description:
              'Mac 専用の音声入力アプリ。完全ローカル処理でプライバシーを守りながら高精度な文字起こしを実現。Apple Silicon 最適化。',
            url: 'https://k1e1n04.github.io/lumina-whisper-site/',
            downloadUrl:
              'https://github.com/k1e1n04/lumina-whisper-site/releases/latest/download/LuminaWhisper.dmg',
            softwareVersion: '0.3.0',
            author: {
              '@type': 'Person',
              name: 'KenIshii',
            },
          })}
        </script>
      </Helmet>
```

- [ ] **Step 2: LandingPage の meta テストが通ることを確認する**

```bash
npm run test:run -- src/test/meta.test.tsx -t "LandingPage"
```

Expected: PASS。

---

## Task 6: ガイドページ + ReleasePage に Helmet を追加する

**Files:**
- Modify: `src/pages/ReleasePage.tsx`
- Modify: `src/pages/guide/InstallPage.tsx`
- Modify: `src/pages/guide/SetupPage.tsx`
- Modify: `src/pages/guide/UsagePage.tsx`
- Modify: `src/pages/guide/SettingsPage.tsx`
- Modify: `src/pages/guide/FaqPage.tsx`
- Modify: `src/pages/guide/TroubleshootPage.tsx`

各ファイルに以下のパターンで `<Helmet>` を追加する。

- [ ] **Step 1: ReleasePage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の要素（`<div className="min-h-screen bg-bg">` の直後）に挿入：
```tsx
      <Helmet>
        <title>リリースノート | Lumina Whisper</title>
        <meta name="description" content="Lumina Whisper の更新履歴。新機能・バグ修正の詳細を版ごとに記載。" />
      </Helmet>
```

- [ ] **Step 2: InstallPage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の `<article ...>` の直後（最初の子要素として）に挿入：
```tsx
      <Helmet>
        <title>インストール | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="macOS 13 以降の Apple Silicon Mac への Lumina Whisper インストール手順。DMGダウンロードからマイク権限設定まで。"
        />
      </Helmet>
```

- [ ] **Step 3: SetupPage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の子要素として挿入：
```tsx
      <Helmet>
        <title>初期設定 | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="Lumina Whisper の初回セットアップ手順。ホットキー確認・音声認識モデルのダウンロード方法を解説。"
        />
      </Helmet>
```

- [ ] **Step 4: UsagePage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の子要素として挿入：
```tsx
      <Helmet>
        <title>基本的な使い方 | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="Option+Space を押すだけで音声入力を開始。テキスト入力フィールドへの自動挿入まで4ステップで解説。"
        />
      </Helmet>
```

- [ ] **Step 5: SettingsPage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の子要素として挿入：
```tsx
      <Helmet>
        <title>設定項目 | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="Lumina Whisper の設定項目一覧。モデル選択・認識言語・ホットキー・LLM後処理の使い方。"
        />
      </Helmet>
```

- [ ] **Step 6: FaqPage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の子要素として挿入：
```tsx
      <Helmet>
        <title>よくある質問 | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="Lumina Whisper に関するよくある質問。対応 macOS・Intel Mac・プライバシー・ホットキーの問題について。"
        />
      </Helmet>
```

- [ ] **Step 7: TroubleshootPage.tsx に Helmet を追加する**

import に追加：
```tsx
import { Helmet } from 'react-helmet-async'
```

`return` の中の最初の子要素として挿入：
```tsx
      <Helmet>
        <title>トラブルシューティング | Lumina Whisper ガイド</title>
        <meta
          name="description"
          content="Lumina Whisper のトラブル対処法。マイク未認識・ホットキー無効・テキスト未挿入の解決手順。"
        />
      </Helmet>
```

- [ ] **Step 8: 全 meta テストが通ることを確認する**

```bash
npm run test:run -- src/test/meta.test.tsx
```

Expected: 8テスト全て PASS。

- [ ] **Step 9: 全テストが通ることを確認する**

```bash
npm run test:run
```

Expected: 全テスト PASS。

- [ ] **Step 10: コミットする**

```bash
git add src/main.tsx src/entry-server.tsx index.html scripts/prerender.mjs \
  src/pages/LandingPage.tsx src/pages/ReleasePage.tsx \
  src/pages/guide/InstallPage.tsx src/pages/guide/SetupPage.tsx \
  src/pages/guide/UsagePage.tsx src/pages/guide/SettingsPage.tsx \
  src/pages/guide/FaqPage.tsx src/pages/guide/TroubleshootPage.tsx \
  src/test/meta.test.tsx
git commit -m "feat(seo): add react-helmet-async per-page meta tags with SSR injection"
```

---

## Task 7: OGP 画像生成スクリプトを作成する

**Files:**
- Create: `scripts/generate-ogp.mjs`
- Modify: `package.json`

- [ ] **Step 1: generate-ogp.mjs を作成する**

`scripts/generate-ogp.mjs` を作成する：

```js
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
import { Resvg } from '@resvg/resvg-js'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const root = path.resolve(__dirname, '..')

// OGP image: 1200x630px, Latin text only (avoids font dependency in CI)
const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#FAFAFA"/>
  <!-- Left accent border -->
  <rect x="0" y="0" width="8" height="630" fill="#863bff"/>
  <!-- Bottom accent line -->
  <rect x="0" y="610" width="1200" height="4" fill="#863bff" opacity="0.25"/>
  <!-- Category label -->
  <text x="80" y="210"
    font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif"
    font-size="22" font-weight="400" fill="#863bff" letter-spacing="4">
    MAC VOICE INPUT APP
  </text>
  <!-- App name -->
  <text x="80" y="310"
    font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif"
    font-size="88" font-weight="600" fill="#1A1A1A">
    Lumina Whisper
  </text>
  <!-- Tagline -->
  <text x="82" y="380"
    font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif"
    font-size="32" font-weight="300" fill="#666666">
    Private · Offline · Apple Silicon
  </text>
  <!-- Requirements -->
  <text x="82" y="555"
    font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif"
    font-size="22" fill="#999999">
    macOS 13.0+ · Apple Silicon · Free
  </text>
</svg>`

const resvg = new Resvg(svg, {
  fitTo: { mode: 'width', value: 1200 },
})
const pngData = resvg.render()
const pngBuffer = pngData.asPng()

const outPath = path.join(root, 'public/ogp.png')
fs.writeFileSync(outPath, pngBuffer)
console.log(`  ✓ ogp.png (${pngBuffer.length} bytes) → ${outPath}`)
```

- [ ] **Step 2: スクリプトの動作確認をする**

```bash
node scripts/generate-ogp.mjs
```

Expected:
```
  ✓ ogp.png (XXXXX bytes) → /Users/ishiiken/Develop/lumina-whisper-site/public/ogp.png
```

`public/ogp.png` が生成され、ファイルサイズが 1KB 以上あることを確認する。

- [ ] **Step 3: package.json の build スクリプトを更新する**

`package.json` の `scripts.build` を以下に変更する：

```json
"build": "node scripts/generate-ogp.mjs && tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs"
```

- [ ] **Step 4: public/ogp.png を .gitignore に追加する（生成物はコミットしない）**

`.gitignore` に以下を追記する：

```
public/ogp.png
```

- [ ] **Step 5: コミットする**

```bash
git add scripts/generate-ogp.mjs package.json .gitignore
git commit -m "feat(seo): add OGP image generation script with @resvg/resvg-js"
```

---

## Task 8: フルビルドを実行して検証する

**Files:**
- Verify: `dist/index.html`, `dist/guide/install/index.html`, `dist/sitemap.xml`

- [ ] **Step 1: フルビルドを実行する**

```bash
npm run build
```

Expected: エラーなく完了。以下のログが出力される：
```
  ✓ ogp.png
  ✓ /
  ✓ /release
  ✓ /guide/install
  ✓ /guide/setup
  ✓ /guide/usage
  ✓ /guide/settings
  ✓ /guide/faq
  ✓ /guide/troubleshoot
  ✓ sitemap.xml
Done.
```

- [ ] **Step 2: LandingPage の静的 HTML にメタタグが注入されていることを確認する**

```bash
grep -o '<title>[^<]*</title>' dist/index.html
```

Expected:
```
<title>Mac 音声入力アプリ — オフライン・高精度 | Lumina Whisper</title>
```

- [ ] **Step 3: ガイドページの静的 HTML にメタタグが注入されていることを確認する**

```bash
grep -o '<title>[^<]*</title>' dist/guide/install/index.html
```

Expected:
```
<title>インストール | Lumina Whisper ガイド</title>
```

- [ ] **Step 4: sitemap.xml の内容を確認する**

```bash
cat dist/sitemap.xml
```

Expected: 8ルートの `<url>` エントリが含まれた XML。先頭が以下の形式：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://k1e1n04.github.io/lumina-whisper-site/</loc>
    <priority>1.0</priority>
  </url>
```

- [ ] **Step 5: robots.txt が dist に含まれることを確認する**

```bash
cat dist/robots.txt
```

Expected:
```
User-agent: *
Allow: /
Sitemap: https://k1e1n04.github.io/lumina-whisper-site/sitemap.xml
```

- [ ] **Step 6: JSON-LD が LandingPage に含まれることを確認する**

```bash
grep -c 'application/ld+json' dist/index.html
```

Expected: `1`（1件ヒット）

- [ ] **Step 7: 全テストが通ることを確認する**

```bash
npm run test:run
```

Expected: 全テスト PASS。

- [ ] **Step 8: 最終コミットをする**

```bash
git add dist/sitemap.xml 2>/dev/null || true
git add public/robots.txt
git commit -m "feat(seo): complete SEO implementation - BrowserRouter, meta tags, OGP, JSON-LD, sitemap"
```

> Note: `dist/` は `.gitignore` に含まれるため dist ファイルはコミット不要。`public/robots.txt` のみコミット対象。

---

## 検証チェックリスト（完了基準）

- [ ] `npm run test:run` — 全テスト PASS
- [ ] `dist/index.html` — `<title>` タグが正しいページタイトルを持つ
- [ ] `dist/guide/install/index.html` — `<title>` タグが `インストール | Lumina Whisper ガイド`
- [ ] `dist/sitemap.xml` — 8ルート全て含む
- [ ] `dist/robots.txt` — Sitemap URL が記載されている
- [ ] `dist/index.html` — `application/ld+json` スクリプトが存在する
- [ ] `public/ogp.png` — ビルド時に生成され 1KB 以上
